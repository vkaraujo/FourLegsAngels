<h1>Pets#index</h1>

<ul class="cards">
  <% @pets.each do |pet| %>
  <li class="card" style="background-image: url('https://source.unsplash.com/random %>')">
    <h2><%= link_to pet.name, pet %></h2>
  </li>
  <% end %>
</ul>